using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Level01 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GameManager.instance.Message("WASD키로 이동, Space키로 점프");
    }
	
	// Update is called once per frame
	void Update () {

	}
}
